
# AI Shopping Agent - Dependencies
# API backend
# Browser automation (Amazon scraper)
# Database
# DuckDuckGo search (no API key required)
# Environment & config
# HTTP scraping (Flipkart scraper)
# LLM providers (selected via LLM_PROVIDER env var)
# aiosqlite>=0.20.0       # uncomment if you want async SQLite in dev
# ──────────────────────────────────────────────
beautifulsoup4>=4.12.0
contextdb[tokens] @ git+https://github.com/HarsimarSingh23/AgentMonitor.git   # observability (AgentMonitor)
ddgs>=9.14.0
fake-useragent>=1.4.0
fastapi>=0.111.0
google-genai>=2.7.0          # provider: gemini
lxml>=4.9.0
openai>=1.30.0               # provider: nvidia (OpenAI-compatible NIM API)
psycopg2-binary>=2.9.9   # PostgreSQL driver (production)
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
requests>=2.28.0
selenium>=4.15.0
setuptools>=70.0.0          # provides distutils for Python 3.12 (needed by undetected-chromedriver)
sqlalchemy>=2.0.30
tenacity>=8.4.0
undetected-chromedriver>=3.5.5
uvicorn[standard]>=0.30.0
