# Amazon AI Shopping Agent - Dependencies
# Core browser automation
selenium>=4.15.0
# webdriver-manager removed — undetected_chromedriver manages its own driver

# Undetected ChromeDriver - bypasses most Amazon bot-detection
undetected-chromedriver>=3.5.5

# Fake User-Agent for spoofing browser fingerprints
fake-useragent>=1.4.0

# Flipkart scraper (lightweight - no browser needed)
requests>=2.28.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# LLM - Google Gemini (free tier) — new SDK
google-genai>=2.7.0

# Env file loading
python-dotenv>=1.0.0

# DuckDuckGo search — no API key required
duckduckgo-search>=6.0.0
